import {useLayoutEffect} from 'react'

import { addClass } from '../format/classNameModifier'

function useModalDom() {
  useLayoutEffect(() => {
    first
  
    return () => {
      
    };
  })
}

export default useModalDom